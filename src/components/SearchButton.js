import React from 'react'
import RaisedButton from 'material-ui/RaisedButton'

const SearchButton = () => {
  return (
    <div>
      <RaisedButton
        label="Submit Search"
        primary={true}
      />
    </div>
  );
}

export default SearchButton
